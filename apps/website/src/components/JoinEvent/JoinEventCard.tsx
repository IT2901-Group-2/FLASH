"use client";
import { Card, Input, Button, Title } from "ui";

const JoinEventCard = () => {

    return (
        <Card data-color="background-secondary">
            <Title as="h2" description="Enter the event code or scan the QR code to join">Join an event</Title>
        </Card>
    )
}

export default JoinEventCard;


